# Informe de tokens — 2026-07-11 (diseño de 001 + gobernanza)

> Foto de la fase de **diseño de la feature 001** (spec/clarify/plan/tasks/analyze + gates G1/G2 hasta
> 0 bloqueantes, Setup Phase 1) y del **hilo de gobernanza** (Principio XV, regla de enmiendas aisladas,
> ADR-0004, re-plan del roadmap). Consumo vía `ccusage`; ahorro vía `rtk gain`. Coste = estimación local.

## ⚠️ Métricas pendientes de ejecución

Este informe se generó en un **sandbox sin `ccusage` ni `rtk`** (no hay Node y el binario RTK no está
instalado aquí). **No se inventan cifras.** Para rellenar las tablas, ejecuta en tu entorno:

```bash
ccusage monthly            # consumo del mes (global)
ccusage daily              # cruzar con la ventana de esta fase (2026-07-10/11)
cd ~/Documents/proyecto-final && rtk gain --project   # ahorro por proyecto
rtk gain --quota -t 20x    # ahorro global + cuota
rtk gain --daily -f json   # serie diaria para aislar esta fase
```

## Consumo (ccusage monthly — global) — _pendiente_

| Mes | Tokens totales | Coste estimado |
|-----|----------------|----------------|
| 2026-07 (en curso) | _ejecutar `ccusage monthly`_ | _—_ |

> Referencia del informe anterior (2026-07-10): 2026-07 en curso iba por **205,497,032 tokens ≈ $186.62**
> (global, todas las sesiones). El delta de esta fase = valor de hoy − ese valor.

## Consumo de ESTA fase (aprox. por ventana temporal) — _pendiente_

- **Ventana:** 2026-07-10 → 2026-07-11.
- **Ramas trabajadas:** `001-fundacion-auth-rbac` (diseño) y `chore/foundation-governance` (gobernanza).
- **Cómo aislarlo:** `ccusage daily` para los días 10 y 11 (RTK/ccusage no etiquetan por rama; se atribuye
  por fecha, ver `informes/README.md §Atribución por spec/fase`).

## Ahorro RTK — proyecto y global — _pendiente_

| Scope | Comandos | Tokens ahorrados | % |
|-------|----------|------------------|---|
| Proyecto (`rtk gain --project`) | _—_ | _—_ | _—_ |
| Global (`rtk gain --quota`) | _—_ | _—_ | _—_ |

## Lectura (cualitativa, no depende de las cifras)

- Esta fase fue **intensiva en razonamiento/redacción** (specify/clarify/plan/tasks/analyze + ~10 rondas
  de panel adversarial G1/G2 + gobernanza), con **poca CLI**. Igual que en la fundación, se espera un
  **ahorro RTK por proyecto modesto** (el ahorro RTK vive en operaciones de shell, no en el razonamiento).
- El grueso del **coste real** (tokens LLM) está en las rondas de gate y clarify, no en herramientas.
- **Aprendizaje de coste** codificado en el Principio XV: specs sobredimensionadas → más rondas de gate →
  más tokens. Dimensionar en slices pequeños **reduce el consumo** de las fases de diseño.
- **No se usó la API de pago** en ningún momento (todo por plan/CLI), coherente con la constitution.

## Próximos informes

- Uno por spec/feature, cruzando la ventana de su rama con `ccusage daily` y `rtk gain --daily`.
- Al implementar 001 en un entorno con Node, medir el coste de la fase de **implementación + G3**
  (previsiblemente más CLI → mayor ahorro RTK por proyecto).
